@hostname = localhost
@port = 3000
@host = {{hostname}}:{{port}}
@contentType = application/json

# POST /users
POST http://{{host}}/api/v1/tasks HTTP/1.1
Content-Type: {{contentType}}

{
    "name": "workout evening",
    "completed": true,
}